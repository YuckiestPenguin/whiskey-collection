<div class="container-fluid">
  <div class="row-fluid">
    <% @whiskeys.each do |whiskey| %>
      <div class="col-sm-6 col-md-3">
        <div class="thumbnail">
          <%= link_to (image_tag whiskey.image.url(:medium), class: "image"), whiskey %>        </div>
      </div>

    <% end %>
  </div>
</div>
